version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "blacklist-task"
        LoadBalancerInfo:
          ContainerName: "blacklist"
          ContainerPort: 5000
